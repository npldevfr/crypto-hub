import { defineConfig, presetUno, presetWebFonts, presetWind } from 'unocss'

export default defineConfig({
    presets: [
        presetUno(),
        presetWebFonts(),
        presetWind()
    ]
})
