import CryptocurrencyProvider from 'App/Models/CryptocurrencyProvider'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(CryptocurrencyProvider, ({ faker }) => {
  return {
    //
  }
}).build()
