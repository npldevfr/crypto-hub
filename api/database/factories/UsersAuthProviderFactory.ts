import users_auth_provider from 'App/Models/users_auth_provider'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(users_auth_provider, ({ faker }) => {
  return {
    //
  }
}).build()
